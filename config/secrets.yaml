development:
  secret_key_base: <%= ENV["SECRET_KEY_BASE_DEV"] || "dev-only-never-use-in-prod-#{'a'*64}" %>

test:
  secret_key_base: test-only-static-key-it-is-fine-here-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>