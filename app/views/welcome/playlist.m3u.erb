#EXTM3U

<%- @items.each do |item| -%>
#EXTINF:<%= item.duration %>, <%= item.playlist_name %>
<%= url_for(:id => item, :controller => :audio_messages, :action => :show, :dl => true, :only_path => false) %>

<%- end -%>
